# MCP Protocol

<details>
<summary>Relevant source files</summary>

The following files were used as context for generating this wiki page:

- [.github/workflows/ci.yml](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/.github/workflows/ci.yml)
- [lib/hermes/client/operation.ex](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/client/operation.ex)
- [lib/hermes/client/request.ex](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/client/request.ex)
- [lib/hermes/mcp/id.ex](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/id.ex)
- [lib/hermes/mcp/message.ex](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex)

</details>



This document covers the Model Context Protocol implementation in hermes-mcp, including message handling, validation, ID generation, and error management. The MCP protocol is built on JSON-RPC 2.0 and provides a standardized way for AI assistants to communicate with external systems.

For information about transport layer implementations that carry MCP messages, see [Transport Layer](#3.2). For client-side protocol usage, see [Client Architecture](#3.3).

## Protocol Overview

The MCP protocol implementation centers around the `Hermes.MCP.Message` module, which provides JSON-RPC 2.0 compliant message handling with MCP-specific extensions. The protocol supports four primary message types: requests, responses, notifications, and errors.

```mermaid
graph TB
    subgraph "JSON-RPC 2.0 Foundation"
        JSONRPC["jsonrpc: '2.0'<br/>Required in all messages"]
    end
    
    subgraph "Message Types"
        Request["Request<br/>method + id + params"]
        Response["Response<br/>result + id"]
        Notification["Notification<br/>method + params"]
        Error["Error<br/>error + id"]
    end
    
    subgraph "MCP Extensions"
        Progress["Progress Tracking<br/>progressToken in _meta"]
        Batch["Batch Processing<br/>Array of messages"]
        Schema["Schema Validation<br/>Peri-based validation"]
    end
    
    JSONRPC --> Request
    JSONRPC --> Response  
    JSONRPC --> Notification
    JSONRPC --> Error
    
    Request --> Progress
    Request --> Batch
    Response --> Batch
    Notification --> Batch
    Error --> Batch
    
    Request --> Schema
    Response --> Schema
    Notification --> Schema
    Error --> Schema
    
    style JSONRPC fill:#f9f9f9
    style Progress fill:#e8f4f8
    style Batch fill:#e8f4f8
    style Schema fill:#e8f4f8
```

Sources: [lib/hermes/mcp/message.ex:1-625](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L1-L625)

## Message Schema System

The protocol uses the Peri library for comprehensive schema validation. Each message type has specific validation rules based on the method being called.

### Core Message Schemas

```mermaid
graph TB
    subgraph "Schema Hierarchy"
        MCPMessage["mcp_message_schema<br/>(union of all types)"]
        
        RequestSchema["request_schema<br/>method + id + params"]
        ResponseSchema["response_schema<br/>result + id"]  
        NotificationSchema["notification_schema<br/>method + params"]
        ErrorSchema["error_schema<br/>error + id"]
        BatchSchema["batch_schema<br/>array of messages"]
    end
    
    subgraph "Method-Specific Schemas"
        InitParams["@init_params_schema"]
        ToolsCallParams["@tools_call_params_schema"]
        ResourcesReadParams["@resources_read_params_schema"]
        ProgressParams["@progress_params"]
    end
    
    subgraph "Validation Functions"
        ParamsParser["params_with_progress_token/1"]
        MethodParser["parse_request_params_by_method/1"]
        NotifParser["parse_notification_params_by_method/1"]
    end
    
    MCPMessage --> RequestSchema
    MCPMessage --> ResponseSchema
    MCPMessage --> NotificationSchema
    MCPMessage --> ErrorSchema
    MCPMessage --> BatchSchema
    
    RequestSchema --> ParamsParser
    ParamsParser --> MethodParser
    MethodParser --> InitParams
    MethodParser --> ToolsCallParams
    MethodParser --> ResourcesReadParams
    
    RequestSchema --> ProgressParams
    NotificationSchema --> NotifParser
```

The schema system dynamically selects parameter validation based on the method field, ensuring type safety for all MCP operations.

Sources: [lib/hermes/mcp/message.ex:83-183](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L83-L183), [lib/hermes/mcp/message.ex:90-108](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L90-L108)

## Message Types and Guards

The protocol provides guard functions to classify messages efficiently during processing:

| Guard Function | Purpose | Condition |
|----------------|---------|-----------|
| `is_request/1` | Identifies requests | Has `method` and `id` fields |
| `is_response/1` | Identifies responses | Has `result` and `id` fields |
| `is_notification/1` | Identifies notifications | Has `method` but no `id` field |
| `is_error/1` | Identifies errors | Has `error` and `id` fields |
| `is_initialize/1` | Identifies init requests | Request with `method` = "initialize" |
| `is_ping/1` | Identifies ping requests | Request with `method` = "ping" |

```mermaid
graph LR
    subgraph "Message Classification"
        IncomingMessage["Incoming JSON"]
        
        RequestGuard["is_request(data)<br/>method + id"]
        ResponseGuard["is_response(data)<br/>result + id"]  
        NotificationGuard["is_notification(data)<br/>method, no id"]
        ErrorGuard["is_error(data)<br/>error + id"]
    end
    
    subgraph "Specialized Guards"
        InitGuard["is_initialize(data)"]
        PingGuard["is_ping(data)"]
        LifecycleGuard["is_initialize_lifecycle(data)"]
    end
    
    IncomingMessage --> RequestGuard
    IncomingMessage --> ResponseGuard
    IncomingMessage --> NotificationGuard
    IncomingMessage --> ErrorGuard
    
    RequestGuard --> InitGuard
    RequestGuard --> PingGuard
    RequestGuard --> LifecycleGuard
```

Sources: [lib/hermes/mcp/message.ex:204-247](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L204-L247), [lib/hermes/mcp/message.ex:260-294](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L260-L294)

## ID Generation System

The `Hermes.MCP.ID` module provides cryptographically unique identifiers for protocol messages. IDs contain temporal, process, and random components to ensure uniqueness across distributed systems.

### ID Structure

```mermaid
graph TB
    subgraph "ID Components"
        Timestamp["Timestamp<br/>64-bit nanoseconds"]
        ProcessHash["Process Hash<br/>24-bit phash2(node, pid)"]
        Random["Random<br/>24-bit random number"]
    end
    
    subgraph "ID Types"
        BaseID["Base ID<br/>Base64(timestamp + process + random)"]
        RequestID["Request ID<br/>'req_' + BaseID"]
        ProgressToken["Progress Token<br/>'progress_' + BaseID"]
        SessionID["Session ID<br/>'session_' + BaseID"]
        BatchID["Batch ID<br/>'batch_' + BaseID"]
        ErrorID["Error ID<br/>'err_' + BaseID"]
    end
    
    subgraph "Validation Functions"
        ValidBase["valid?/1"]
        ValidRequest["valid_request_id?/1"]
        ValidProgress["valid_progress_token?/1"]
        TimestampExtract["timestamp_from_id/1"]
    end
    
    Timestamp --> BaseID
    ProcessHash --> BaseID
    Random --> BaseID
    
    BaseID --> RequestID
    BaseID --> ProgressToken
    BaseID --> SessionID
    BaseID --> BatchID
    BaseID --> ErrorID
    
    BaseID --> ValidBase
    RequestID --> ValidRequest
    ProgressToken --> ValidProgress
    BaseID --> TimestampExtract
```

The ID generation functions ensure uniqueness through:
- **Temporal uniqueness**: Nanosecond timestamps prevent time-based collisions
- **Process uniqueness**: Hash of node and PID prevents process-based collisions  
- **Random uniqueness**: 24-bit random component prevents algorithmic collisions

Sources: [lib/hermes/mcp/id.ex:1-244](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/id.ex#L1-L244)

## Protocol Methods

The MCP protocol defines specific methods for different types of operations:

### Core Methods

```mermaid
graph TB
    subgraph "Initialization"
        Initialize["initialize<br/>Protocol handshake"]
        InitNotif["notifications/initialized<br/>Handshake complete"]
    end
    
    subgraph "Capability Discovery"  
        ToolsList["tools/list<br/>List available tools"]
        PromptsList["prompts/list<br/>List available prompts"]
        ResourcesList["resources/list<br/>List available resources"]
        RootsList["roots/list<br/>List workspace roots"]
    end
    
    subgraph "Execution"
        ToolsCall["tools/call<br/>Execute a tool"]
        PromptsGet["prompts/get<br/>Get prompt template"]
        ResourcesRead["resources/read<br/>Read resource content"]
        CompletionComplete["completion/complete<br/>Get completions"]
    end
    
    subgraph "Management"
        Ping["ping<br/>Health check"]
        LoggingSetLevel["logging/setLevel<br/>Set log level"]
        Cancel["notifications/cancelled<br/>Cancel operation"]
        Progress["notifications/progress<br/>Progress update"]
        Message["notifications/message<br/>Log message"]
    end
    
    Initialize --> InitNotif
    ToolsList --> ToolsCall
    PromptsList --> PromptsGet
    ResourcesList --> ResourcesRead
```

Each method has specific parameter schemas enforced by the validation system.

Sources: [lib/hermes/mcp/message.ex:12-13](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L12-L13), [lib/hermes/mcp/message.ex:97-108](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L97-L108)

## Progress Tracking

The protocol supports progress tracking for long-running operations through progress tokens and notifications.

### Progress Token Flow

```mermaid
sequenceDiagram
    participant Client
    participant Server
    
    Note over Client,Server: Request with Progress Token
    Client->>Server: Request with _meta.progressToken
    Server->>Client: Immediate Response (operation started)
    
    Note over Client,Server: Progress Updates
    Server->>Client: notifications/progress (25%)
    Server->>Client: notifications/progress (50%)  
    Server->>Client: notifications/progress (75%)
    Server->>Client: notifications/progress (100%)
    
    Note over Client,Server: Optional Cancellation
    Client->>Server: notifications/cancelled
```

Progress notifications can include optional `message` field for descriptive updates (2025-03-26 specification).

Sources: [lib/hermes/mcp/message.ex:77-81](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L77-L81), [lib/hermes/mcp/message.ex:115-127](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L115-L127), [lib/hermes/mcp/message.ex:419-432](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L419-L432)

## Batch Processing

The protocol supports JSON-RPC batch processing for sending multiple messages in a single transmission:

```mermaid
graph TB
    subgraph "Batch Structure"
        BatchArray["JSON Array<br/>[message1, message2, ...]"]
        Message1["Request/Response/<br/>Notification/Error"]
        Message2["Request/Response/<br/>Notification/Error"]
        MessageN["Request/Response/<br/>Notification/Error"]
    end
    
    subgraph "Processing"
        BatchSchema["batch_schema validation"]
        IndividualValidation["Individual message validation"]
        BatchResponse["Batch response array"]
    end
    
    BatchArray --> Message1
    BatchArray --> Message2
    BatchArray --> MessageN
    
    BatchArray --> BatchSchema
    BatchSchema --> IndividualValidation
    IndividualValidation --> BatchResponse
```

Batch processing enables efficient communication by reducing round-trips and allowing parallel processing of multiple operations.

Sources: [lib/hermes/mcp/message.ex:184-185](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L184-L185), [lib/hermes/mcp/message.ex:544-553](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L544-L553)

## Error Handling

The protocol implements comprehensive error handling following JSON-RPC 2.0 specifications:

```mermaid
graph TB
    subgraph "Error Structure"
        ErrorMessage["Error Message"]
        JSONRPC["jsonrpc: '2.0'"]
        ErrorField["error field"]
        ID["id field"]
    end
    
    subgraph "Error Details"
        Code["code: integer<br/>Standard error codes"]
        Message["message: string<br/>Human readable description"]
        Data["data: any<br/>Additional error info"]
    end
    
    subgraph "Error Types"
        ValidationError["Validation Error<br/>Invalid message format"]
        MethodError["Method Error<br/>Unknown method"]
        ParamsError["Parameters Error<br/>Invalid parameters"]
        InternalError["Internal Error<br/>Server-side error"]
    end
    
    ErrorMessage --> JSONRPC
    ErrorMessage --> ErrorField
    ErrorMessage --> ID
    
    ErrorField --> Code
    ErrorField --> Message
    ErrorField --> Data
    
    ValidationError --> ErrorMessage
    MethodError --> ErrorMessage
    ParamsError --> ErrorMessage
    InternalError --> ErrorMessage
```

Sources: [lib/hermes/mcp/message.ex:165-173](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L165-L173), [lib/hermes/mcp/message.ex:493-500](https://github.com/cloudwalk/hermes-mcp/blob/8db7a927/lib/hermes/mcp/message.ex#L493-L500)

## Message Encoding and Decoding

The protocol provides comprehensive encoding and decoding functions with schema validation:

### Encoding Functions

| Function | Purpose | Output |
|----------|---------|---------|
| `encode_request/2` | Encode request messages | JSON string with newline |
| `encode_response/2` | Encode response messages | JSON string with newline |
| `encode_notification/1` | Encode notifications | JSON string with newline |
| `encode_error/2` | Encode error messages | JSON string with newline |
| `encode_batch/1` | Encode batch messages | JSON array with newline |
| `encode_progress_notification/1` | Encode progress updates | JSON string with newline |

### Decoding Process

```mermaid
flowchart TD
    RawData["Raw Input Data"]
    SplitLines["Split by newlines"]
    ParseJSON["Parse each JSON line"]
    ValidateMessages["Validate against schemas"]
    ReturnResult["Return validated messages"]
    
    RawData --> SplitLines
    SplitLines --> ParseJSON
    ParseJSON --> ValidateMessages
    ValidateMessages --> ReturnResult
    
    ValidateMessages -->|Invalid| ErrorReturn["Return error"]
```

All encoding functions append newline characters for compatibility with line-based transport protocols.